/// <reference types="node" />
import * as http from 'http';
import * as http2 from 'http2';
import { Body } from '../response';
/**
 * A node.js Http request
 */
export declare type NodeHttpRequest = http.IncomingMessage | http2.Http2ServerRequest;
/**
 * A node.js Http response
 */
export declare type NodeHttpResponse = http.ServerResponse | http2.Http2ServerResponse;
/**
 * A type guard to see if a Response object is a HTTP2 response.
 */
export declare function isHttp2Response(response: NodeHttpResponse): response is http2.Http2ServerResponse;
export declare function sendBody(res: NodeHttpResponse | http2.Http2Stream, body: Body): void;
/**
 * The HttpCallback is the function that is passed as a request listener to
 * node.js's HTTP implementations (http, https, http2).
 */
export declare type HttpCallback = (req: NodeHttpRequest, res: NodeHttpResponse) => void;
