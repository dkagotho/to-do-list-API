import { Middleware } from '@curveball/core';
declare type AccessLogOptions = {
    blacklist?: string[];
    disableColor?: boolean;
    forceColor?: boolean;
};
export declare function accessLog(blacklist?: string[]): Middleware;
export declare function accessLog(options: AccessLogOptions): Middleware;
export default function accessLog(arg?: string[] | AccessLogOptions): Middleware;
export {};
