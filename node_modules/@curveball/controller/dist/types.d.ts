/**
 * A simple type to gather annotations on any method
 */
export declare type MethodAnnotation = {
    name: string;
    args: string[];
};
/**
 * The route table has HTTP methods for key names
 */
export declare type RouteTable = Map<string, MethodRoutes>;
declare type MethodRoutes = {
    accepts: Map<string, string>;
    default: string | null;
};
export {};
